const routes = {
    home: '/',
    about: '/about',
    login: '/login',
    register: '/register',
    loginP: '/loginp',
    tasks: '/tasks',
    task: '/tasks/:id',
    admin: {
        users: '/admin/users'
    }
}

export default routes